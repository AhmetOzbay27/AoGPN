# P0 Handover Note — 2026-09-03

Handover document for the AoGPN refactor ("Freebuff Desktop" repo). Written at the end of the
session so a **new chat tomorrow** can resume without re-deriving context. Repo root contains the
`AoGPN/` solution folder (AoGPN.sln); code lives under `AoGPN/AoGPN` (WPF app) and
`AoGPN/ServiceLib` (+ `AoGPN/ServiceLib.Tests`). Turkish is the dominant language of the codebase
comments; work so far has been verified with `dotnet build` / `dotnet test` from `AoGPN/`.

---

## 1. Hard guard rules (never break)

1. **`ServiceLib/Services/Gpn/` (WinDivert/Wintun/WireGuard classes): DO NOT TOUCH.** That layer is
   the future Tier-1 target and is protected.
2. **AmazTool: never delete or move** (runtime call sites exist).
3. Never commit/push unless the user asks. Working tree is a **shared local checkout** — other
   agents / the user may edit the same files mid-session (this happened several times). Always
   re-check `git status` + the file before consequential work; never stage broad changes.
4. Business rules in the decision engine must stay **byte-identical** when moved: Smart Fallback,
   15 ms hysteresis, UDP/ICMP matrix, hairpin policy, ping-pong cooldown, slow-server tolerance.

---

## 2. Completed work (this session, 3 phases)

### P1 — Quarantine + solution hygiene (DONE, verified)
- Moved orphaned `GlobalHotKeys/` project + scratch artifacts (`_analyze*.ps1`, `captures/`,
  `tmp-mihomo/`, `.icon-inspect/`, …) into `Silinecekler_Yedek/` (gitignored). Mostly done by an
  earlier pass; re-verified. **`reflect-downloader.cs` was already gone from git history — not recoverable.**
- Edited `AoGPN.sln` + `AoGPN.slnx`: removed GlobalHotKeys, added the 3 tool projects
  (`AoGPN.GpnJsonlConsumer`, `AoGPN.GpnProbeTool`, `AoGPN.GpnSessionTool`) → both solutions now
  carry the identical set of projects.
- `.gitignore`: added `.icon-inspect/` and `Silinecekler_Yedek/`.

### Test-suite repair (DONE — 1023 green)
- 33× `no such table: RoutingItem` fixed by adding idempotent
  `SQLiteHelper.Instance.CreateTable<RoutingItem>()` before first query in **10 test fixtures**
  (some hunks landed via a concurrent actor mid-session; all were verified).
- 8× sing-box 1.14.0 `dns rule: response-based matching requires a preceding evaluate action`
  failures disappeared when the working tree reverted the uncommitted DNS refactor to the HEAD
  shape (HEAD shape passes the real binary `check`).

### P0 Wave 1 — MainWindow message dispatch extraction (DONE, verified)
- `AoGPN/AoGPN/Services/IDashboardBridge.cs` (new, ~121 lines): window-owned operation surface.
- `AoGPN/AoGPN/Services/DashboardMessageDispatcher.cs` (new, ~1,189 lines): owns the WebView2
  dashboard conversation — `HandleWebMessageReceived` (78-case switch), JSON decoders, thin
  forwarders to the bridge. Subscribed from MainWindow ctor/Closed.
- `MainWindow.xaml.cs`: **7,071 → ~6,161 lines** at end of Wave 1. Switch body + decoders moved;
  ops stayed on the window behind `IDashboardBridge` (58 members publicized).
- Verified: 78/78 switch cases transferred; moved statics byte-identical.

### P0 Wave 2a — Decision engine split (DONE, verified)
`ServiceLib/Services/GpnServerSelectionService.cs` **1,753 → 861 lines**; three new files:
- `GpnDecision.cs` (internal static, 430 l.): DecideMode / DecideSelection / DecideFailover /
  DecideRecovery / IsPingPongSwitchSuppressed / IsUdpHealthy|DeadStatus / OrderCandidates /
  HasNonHairpinAlternative / ToPolicy + `FailoverActionType` + `FailoverDecision`.
- `GpnHairpinDetector.cs` (internal, 203 l.): IsHairpin + own-IP resolution + 60 s caches.
- `GpnServerProber.cs` (internal, 376 l.): ProbeAll/ProbeUdpAll/ProbeUdp + ICMP/TCP/handshake
  measurement core.
- Orchestrator keeps public API as forwarding wrappers; ctor now `internal` (sub-layer types
  internal) and DI builds it with a factory.
- `ServiceLib/DI/GpnServiceCollectionExtensions.cs`: registers `GpnServerProber` +
  `GpnHairpinDetector` singletons; `IGpnServerSelectionService` registered via factory lambda.
- Callers updated: `GpnServerSelectionServiceTests.cs` (79 static refs → `GpnDecision.*` /
  `GpnHairpinDetector.*`), `AoGPN.GpnProbeTool/Program.cs`.
- Verified: all 25 moved members byte-identical vs HEAD (modulo visibility).

### P0 Wave 2b — MainWindow business-logic migration (DONE, verified)
`AoGPN/AoGPN/Views/MainWindow.xaml.cs`: **6,161 → 5,271 lines**. Two new Services-layer classes
(+ DI registration), 25 members migrated, bodies byte-identical:
- `AoGPN/Services/DashboardSettingsService.cs` (647 l.): full Settings cluster —
  PushSettingsAsync, SaveSettingsAsync → SaveSettingsCoreAsync, SaveSettingsCoreTypes,
  NotifySettingsSaveAsync, GetSettings* decoders, AllowEnableTun, PushEffectsTierAsync,
  PushLanguageAsync, PushAppInfoAsync, PushSystemProxyStateAsync (+ its `_lastSystemProxySignature`
  state).
- `AoGPN/Services/DashboardGpnServerService.cs` (398 l.): GPN server catalogue —
  PushGpnServersAsync, ProbeGpnServersAsync, PushGpnFailoverMatrixAsync,
  PushGpnSelectionPredictionAsync, RestoreGpnDefaultsAsync, PushGpnDefaultsStatusAsync,
  ImportGpnServersAsync, DeleteGpnServerAsync, ToggleGpnServerAsync, NotifyGpnServersOpAsync.
- `AoGPN/DI/AoGpnDashboardServiceCollectionExtensions.cs` (new): `AddAoGpnDashboardServices(...)`
  registers both as singletons (composition-root-ready; runtime still composes manually in
  MainWindow ctor, same pattern as ServiceLib.DI).
- Design: services touch the UI only through injected delegates (`executeScript` bound to
  `MainWindow.ExecuteScriptSafelyAsync` — the dispatcher-marshaling choke point stays in the
  window; plus `isWebViewReady`, `readTransport`, `getViewModel`). Dialogs/tray/lifecycle stay in
  MainWindow (`ShowGpnServerEditDialogAsync` kept; private members with window callers became
  one-line wrappers). Public members became one-line delegations → `IDashboardBridge` and the
  dispatcher unchanged.
- Dispatcher static forwarders repointed: `MainWindow.GetSettingsBool/GetSettingsInt/
  NormalizeEffectsMode` → `DashboardSettingsService.*`.

---

## 3. Current verification state (all green at session end)

- `cd AoGPN && dotnet build AoGPN.sln` → **0 errors, 0 warnings**.
- `cd AoGPN && dotnet test ServiceLib.Tests/ServiceLib.Tests.csproj` →
  **Başarılı: 0 failed / 1,023 passed / 5 skipped** (the 5 skips are pre-existing
  WinDivert/Wintun/WireGuard driver tests needing admin/driver context — not failures).

---

## 4. Working-tree ownership map (NOTHING is committed — check before any commit)

Changes made by **me** this session:
- `.gitignore`, `AoGPN/AoGPN.sln`, `AoGPN/AoGPN.slnx`
- `AoGPN/AoGPN/Views/MainWindow.xaml.cs`
- New: `AoGPN/AoGPN/Services/{IDashboardBridge,DashboardMessageDispatcher,DashboardSettingsService,DashboardGpnServerService}.cs`, `AoGPN/AoGPN/DI/AoGpnDashboardServiceCollectionExtensions.cs`
- New: `ServiceLib/Services/{GpnDecision,GpnHairpinDetector,GpnServerProber}.cs`
- `ServiceLib/Services/GpnServerSelectionService.cs`, `ServiceLib/DI/GpnServiceCollectionExtensions.cs`
- `ServiceLib.Tests/Services/GpnServerSelectionServiceTests.cs`, `AoGPN.GpnProbeTool/Program.cs`

Pre-existing / concurrent-actor changes (NOT mine — do not attribute or blindly stage):
- In-flight ServiceLib refactor (routing/DNS/singbox work) visible before I started.
- `AoGPN/AoGPN.csproj`, icons/Splash.png, `ServiceLib/Manager/CertPemManager.cs`,
  `ServiceLib/Services/CoreInstaller.cs`, `UpdateService.cs`, `DownloaderHelper.cs` +
  `DownloaderHelperResumeTests.cs` (new), `Temalar/skins/nexus/skin.html`,
  `vpn-gpn-dashboard.html`, `make-core-bundles.ps1` (new, repo root), `.gitmodules` deletion
  (deleted), plus the P1 quarantined GlobalHotKeys deletions (intended).

---

## 5. What remains (tomorrow's roadmap)

**P0 still open — main goal: push `MainWindow.xaml.cs` toward a pure UI-lifecycle shell.**
Recommended order:
1. **Node/profile cluster → `DashboardNodeService`**: SelectNodeAsync, Copy/Paste/DeleteNodes,
   Disable/Restore, CleanupFailed, Dedup, ToggleNodeFav, node-pool link ops, PushNodeListAsync,
   PushNodeInfoAsync, PushNodePoolAsync (~700 lines). Watch: dialog confirmations, speedtest
   bridge (`_nodeSpeedtestService`, `_nodeTestRunId`), `_lastNodeSignature`.
2. **Egress push cluster → `DashboardPushService`**: telemetry/resilience/monitor/drift pushes
   (~600 lines) sharing script channel via injected delegate.
3. **Connection/tray cluster** — **KAPANDI (belgeleme dalı, 2026-09-05):** karar çekirdekleri
   zaten ServiceLib'de/testli (ConnectionCoordinator, GpnConnectionCoordinator, TrayWindowCoordinator,
   WindowLifecycleDecisionService, RealityCoreFallbackAdvisor); MainWindow'da kalan ~2.400 satır
   metot gövdesi durum makinesi değil, WPF kromu + WebView2 DOM yapıştırıcısıdır. Tam model +
   gerekçe + ileri taşınma blueprint'i: `docs/connection-tray-cluster.md`.
4. Repeat same recipe as Wave 2b: cut spans byte-exact → new service → one-line delegations on
   the window → private wrappers for kept members → repoint dispatcher statics → build to 0 →
   body-diff moved members vs pre-move → full test suite.

**Verification recipe used every wave:** build 0 errors → mechanical body-diff of every moved
member against the pre-move text (visibility/context-rename normalized) → `dotnet test` expecting
1,023 green. Never trust "it compiles" alone; the UI has no automated tests, so byte-identical
moves + compiler-driven rewiring are the safety net.

---

## 6. Single resume prompt (paste into tomorrow's new chat)

```text
Context: Freebuff Desktop (AoGPN) repo — P0 refactor in progress, nothing committed. Session
3 Sep finished: (1) P1 quarantine + solution hygiene (GlobalHotKeys removed from AoGPN.sln/.slnx,
3 tool projects added, .gitignore updated); (2) ServiceLib test repair → 1,023 green (RoutingItem
CreateTable fixtures; sing-box DNS generator reverted to HEAD); (3) P0 Wave 1: MainWindow.xaml.cs
7,071→6,161 by extracting DashboardMessageDispatcher + IDashboardBridge (78-case switch moved);
(4) P0 Wave 2a: GpnServerSelectionService.cs 1,753→861 — pure decisions → GpnDecision.cs
(internal static), probing → GpnServerProber.cs, hairpin → GpnHairpinDetector.cs, DI factory in
ServiceLib/DI; (5) P0 Wave 2b: MainWindow.xaml.cs 6,161→5,271 — Settings cluster →
DashboardSettingsService.cs (647 l.) and GPN-server catalogue → DashboardGpnServerService.cs
(398 l.), both under AoGPN/AoGPN/Services with ctor-injected UI delegates (executeScript bound to
ExecuteScriptSafelyAsync, isWebViewReady, readTransport, getViewModel); dialogs/tray stay on the
window; public members are one-line delegations, IDashboardBridge + dispatcher unchanged; DI
registration added at AoGPN/AoGPN/DI/AoGpnDashboardServiceCollectionExtensions.cs. Full
handover: HANDOVER_P0_2026-09-03.md in the repo root.

Guard rules: never touch ServiceLib/Services/Gpn (WinDivert/Wintun/WireGuard); never delete/move
AmazTool; shared checkout — verify git status before and during edits; keep decision-engine rules
byte-identical (Smart Fallback, 15 ms hysteresis, UDP/ICMP matrix, hairpin, cooldown, tolerance).
Verification gate: dotnet build 0 errors + byte-diff every moved member vs pre-move text +
dotnet test ServiceLib.Tests → 0 failed / 1,023 passed / 5 skipped.

Next task (P0 Wave 3): migrate the node/profile-management cluster from MainWindow.xaml.cs
(SelectNodeAsync, Copy/Paste/Delete/Disable/RestoreNodes, CleanupFailedNodes, DedupNodes,
ToggleNodeFav, node-pool link ops, PushNodeListAsync/PushNodeInfoAsync/PushNodePoolAsync) into a
new AoGPN/AoGPN/Services/DashboardNodeService.cs using the exact Wave-2b recipe; then continue
with the egress-push cluster (DashboardPushService) toward a MainWindow.xaml.cs pure UI
lifecycle/dialog shell well under ~4,000 lines. Report line counts + test results.
```

---

## 6. Wave 3 addendum — 2026-09-05 (DONE, verified)

Both remaining roadmap clusters were extracted with the exact Wave-2b recipe:
byte-exact bodies (verified via pre-move sed snapshots + normalized diff), ctor-injected UI
delegates, one-line window delegations, dispatcher + IDashboardBridge unchanged.

**Wave 3a — DashboardNodeService** (`AoGPN/AoGPN/Services/DashboardNodeService.cs`, new):
SelectNodeAsync, Copy/Paste/DeleteNodesAsync, StartNodeSpeedtestAsync + PushNodeTestResultAsync +
StopNodeSpeedtest (speedtest bridge state `_nodeSpeedtestService/_nodePingCancellation/`
`_nodeTestRunning/_nodeTestRunId` moved with it; window keeps `IDashboardBridge.NodeTestRunId` as
one-line delegation), Disable/Restore/CleanupFailed/DedupNodesAsync, PushNodePoolAsync,
Add/Edit/Remove/FetchNodePoolLinkAsync, NormalizeNodePoolContent/CollectNodeUris/IsNodeUri,
ToggleNodeFavAsync, PushNodeInfoAsync, PushNodeListAsync (+ `_lastNodeSignature`). Injected
delegates: executeScript, isWebViewReady, isClosing, notifyNodesOp (= window
`NotifyNodesOpAsync`, kept on the window because `SystemProxyOnlyService` ctor binds it),
getProfilesViewModel, invokeOnUiThread (= Dispatcher.InvokeAsync), proxyOnlyService,
pushSystemProxyState, updateTrayStatus.

**Wave 3b — DashboardPushService** (`AoGPN/AoGPN/Services/DashboardPushService.cs`, new):
PushGpnDiagAsync, PushGpnTelemetryAsync, PushGpnCaptureStatsAsync (+ `_lastCaptureStats` with
UpdateLastCaptureStats feed), GetGpnPidBridge/GpnPidBridge + PushGpnPidPoolAsync (+`_gpnPidBridge`,
DisposePidBridge for window Closed), PushWarpHealthAsync/PushWinDivertHealthAsync,
PushGpnResilienceLogAsync, PushGpnDrainAsync, PushGpnResilienceAsync (StatusBar tray writes move
unchanged — singleton), PushAvailabilityInfoAsync, PushRuleDriftAsync (+`_lastRuleDriftVerdict`),
PushProcessCatalogAsync, PushMonitorSnapshotAsync; also owns `_gpnTelemetry/_gpnResilienceLog/`
`_processCatalogService` and exposes ResetGpnTelemetry/ClearGpnResilienceLog/
TryResolveExecutablePath. Injected: executeScript, isWebViewReady, getConnectionViewModel,
readTransport, readActualConnectionState, getActiveView, getWebViewToken,
getBypassEgressController (lazy — created in WhenActivated).

Line counts: MainWindow.xaml.cs **5,317 → 3,999** (−1,318; original 7,071 → 3,999). Both
services registered in `AoGPN/AoGPN/DI/AoGpnDashboardServiceCollectionExtensions.cs`
(composition-root-ready; runtime still composes manually in MainWindow ctor). WPF orphan stray
doc comment above PushGpnDiagAsync removed. Moved-member diff = only visibility changes
(private→public for wrappers) + mechanical delegate renames; one CS4014 silenced with an async
lambda, one CS0219 cleaned in GpnBypassEgressController.

Verification: `dotnet build AoGPN.sln` → **0 errors** (app project: 2 pre-existing CS4014 in
MainWindow ctor only); `dotnet test ServiceLib.Tests` → **0 failed / 1,143 passed / 5 skipped**
(one environment-timing flake on a first run re-passed green on rerun; the WPF app is not
referenced by ServiceLib.Tests).

Remaining roadmap: the **connection/tray cluster** — **resolved on 2026-09-05 via the
"keep — document why" branch**: the decision cores were already extracted and tested in
ServiceLib (see §3 of `docs/connection-tray-cluster.md`); the ~2,400 remaining MainWindow lines
are window-bound presenter/event glue (WPF chrome, tray icon lifetime, DOM channel ownership,
global-state reads). Full state-machine models per sub-flow + rationale + a forward-migration
blueprint live in `docs/connection-tray-cluster.md`.

---

## 7. Session addendum — 2026-09-05 (öneri uygulamaları + A2 + güncel durum)

Wave 3 ekinden (§6) sonra aynı oturumda dört öneri ve risk analizinin son açık maddesi (A2)
uygulandı. Hepsi **commit'siz**, çalışma ağacında — §8'deki bekleyen işlere bak.

### Öneri 1 — WARP degrade-egress (launcher bypass sağlamlaştırma)
- `ServiceLib/Services/GpnBypassEgressController.cs` (yeni): WARP egress faulted olduğunda BSG
  launcher istekleri doğrudan DIRECT'e düşer; sağlıklı dönüşte otomatik WARP'a geri döner
  (degrade → recovery döngüsü, toast + dashboard rozeti).
- Mihomo tarafı: `GpnMihomoConfigService` (bsgRuleTarget) + `GpnSoftRouting`
  (BsgGroupName / ResolveBsgEgressTarget / BsgMemberOrder).
- Dashboard: degraded rozeti + sözlük anahtarları (9 dil).
- Testler: `GpnBypassEgressControllerTests` (9) + GpnSoftRoutingTests / GpnMihomoConfigServiceTests
  uzantıları; JS tarafı 211/211.

### Öneri 2 — Maç ortası restart erteleme
- Yapısal değişiklik (liste/kural değişimi) sırasında bağlantı kesilmez; erteleme sonraki doğal
  yeniden bağlantıya bırakılır, toast ile kullanıcı bilgilendirilir.
- `GpnSoftRouting.IsStructuralEntryChange` + ResUI iletileri.
- Testler: GpnSoftRoutingTests uzantıları.

### Öneri 3 — Fmt URI round-trip testleri
- `ServiceLib.Tests/Fmt/FmtUriRoundTripTests.cs` (yeni, 3 theory / 15 vaka): 6 yeni protokol
  (TUIC/Naive/Anytls/Socks vb.) + REALITY detayları — URI ayrıştırma → yeniden üretim eşleşmesi;
  ilk koşuda yeşil.

### Öneri 4 — SpeedtestService regresyonları + gerçek hata düzeltmesi
- `ServiceLib.Tests/Services/SpeedtestServiceTests.cs` (yeni, 7 test): hayalet-Stop koruması,
  kısmi sonuç kalıcılığı, 64'lük parti sınırı, çift-tıklama koruması (`HasActiveRun`).
- Düzeltilen gerçek hata: boş seçimde `pageSize=0` → `GetTestBatchItem` içinde 0/0 → NaN →
  `(int)` **OverflowException**; boş koşu "tamamlandı" yerine "durduruldu" raporluyordu.
  Erken-çıkış koruması (`Count==0 || pageSize<=0`) Tcping/Realping/UDP üçünü de kapsar.

### A2 — Yakalama boşluğu (capture gap) sağlamlaştırma
- **İlk-tik PID yakalama**: `GpnTargetResolver.RefreshLoopAsync`'a `firstTickInterval` parametresi;
  `GpnCaptureLoop` bağlantı sırasında doğan oyunları ~750 ms'de (5 sn yerine) WinDivert filtresine
  alır; PID seti değişmezse sessiz kalır (filtre yeniden derlenmez). `GpnTargetResolverRefreshTests` (2).
- **Sürüklenme uyarısı**: `ServiceLib/Services/GpnCaptureDriftChecker.cs` (yeni, saf, 87 satır)
  — "canlı bağlantı var ama 0 paket yakalanıyor" tespiti; `DashboardPushService.PushMonitorSnapshotAsync`
  per-uygulama drift hesabı yapar (yalnızca `routingMode=="gpn"` ve tunneled uygulamalar — yanlış
  pozitif yok); dashboard'da rose uyarı bandı (`captureDriftBanner`) + `capture.driftBanner` (9 dil).
  `GpnCaptureDriftCheckerTests` (8).
- Not: köprü tüneli açmadan **önce** PID çözüyordu (`resolver.Resolve(...)` → `_tunnel.Open(...)`);
  kapatılan boşluk yeniden çözümleme penceresiydi — korunan Gpn katmanına minimal dokunuş.

### Güncel satır sayıları (2026-09-05, `wc -l`)
| Dosya | Satır |
|---|---:|
| `AoGPN/AoGPN/Views/MainWindow.xaml.cs` | **3.999** (7.071'den −3.072; sonraki işler pencereye dokunmadı) |
| `AoGPN/AoGPN/Services/DashboardMessageDispatcher.cs` | 1.189 |
| `AoGPN/AoGPN/Services/DashboardNodeService.cs` | 969 |
| `AoGPN/AoGPN/Services/DashboardPushService.cs` | 659 (A2 drift koduyla büyüdü) |
| `AoGPN/AoGPN/Services/DashboardSettingsService.cs` | 647 |
| `AoGPN/AoGPN/Services/DashboardGpnServerService.cs` | 398 |
| `AoGPN/AoGPN/Services/IDashboardBridge.cs` | 121 |
| `AoGPN/ServiceLib/Services/GpnCaptureDriftChecker.cs` | 87 |
| `AoGPN/ServiceLib/Services/GpnServerSelectionService.cs` | 861 |

### Güncel doğrulama (bu bölüm yazılırken tekrar koşuldu)
- `dotnet build AoGPN.sln -c Debug` → **0 hata / 0 uyarı** (uygulama projesinde yalnızca 2 önceden
  var olan MainWindow CS4014).
- `dotnet test ServiceLib.Tests` → **0 başarısız / 1.153 geçti / 5 atlandı** (toplam 1.158;
  orijinal el kitabı baz çizgisi 1.023 → bu oturumda +130; 5 atlama önceden var olan sürücü testleri).
- Dashboard JS testleri (`Temalar/skins`, node:test) → **211/211**.
- Yeni test dosyaları: FmtUriRoundTripTests (15 vaka), SpeedtestServiceTests (7),
  GpnCaptureDriftCheckerTests (8), GpnTargetResolverRefreshTests (2), GpnBypassEgressControllerTests (9).

### §8 Bekleyen işler (yarım kalan)
1. **Commit bölme** (kullanıcı isteği, yarım): oturum değişiklikleri mantıklı commit'lere
   ayrılıyor — hunk-splitter (`split.mjs`) + index-stager (`stage.mjs`) yazıldı, tüm karışık
   dosyaların hunk'ları sınıflandırıldı (Ö1/Ö2/A2/P0/PRE), stager'ın konum-kayması hatası
   düzeltildi; commit 1 (WARP degrade-egress) staging'i sırasında oturum kesildi. Çalışma
   ağacında **71 değişiklik, hiçbiri commit'li değil**. Dikkat: bazı dosyalar oturum öncesi
   (Tier-4 dönemi, bize ait olmayan) değişiklikler de içeriyor — yalnızca kendi hunks'lerimiz
   stage edilmeli; hazır patch'ler `/tmp/p0commit/*.patch` (repo dışı, Windows temp'te).
2. **Launcher-bypass genelleştirme** (kullanıcı isteği, keşif aşamasında): `BsgLauncherDomains`
   sabit listesini kullanıcı düzenlenebilir, launcher başına domain + egress tanımlı genel bir
   özelliğe dönüştürme — GpnMihomoConfigService / GpnSoftRouting / GpnBypassEgressController /
   dashboard UI haritalandı; uygulama yazılmadı.
3. **Bağlantı/tepsi kümesi** — **KAPANDI (2026-09-05, belgeleme dalı):** alt akışlar durum
   makineleri olarak modellendi (çekirdeklerin çoğu zaten ServiceLib'de/testli) ve kalma gerekçesi
   belgelendi — `docs/connection-tray-cluster.md` (envanter, 4.1-4.5 durum modelleri, §5 gerekçe,
   §6 istenirse taşıma blueprint'i).
4. **Wave 4 çıkarım adayları** (analiz tamam, 2026-09-05): MainWindow'un kalan en büyük
   blokları ölçüldü (18 bölge; bağlantı kolları 1.323, IP/ISP 286, telemetri 264, hata kartı 210,
   ctor 438) ve öncelikli adaylar belgelendi — `docs/mainwindow-wave4-candidates.md` (W4-A poll
   süpervizörü, W4-B hata defteri, W4-C IpLeakVerifier, W4-D bağlantı orkestratörü [yüksek risk,
   ayrı dalga], W4-E Reality orkestrasyonu, W4-F proxy testi; beklenen kazanç ~300-450 satır,
   W4-D ile ~1.000-1.200). Uygulama başlamadı.

### Tek-cümle özet (yeni sohbet için)
Wave 3'ten sonra dört öneri + A2 bitti: WARP degrade-egress (GpnBypassEgressController),
maç ortası restart erteleme, Fmt URI round-trip (15 vaka), SpeedtestService OverflowException
fiks + 7 regresyon testi, ilk-tik PID yakalama + capture-drift uyarısı (GpnCaptureDriftChecker,
8 test). Doğrulama: build 0 hata / 0 uyarı, ServiceLib.Tests 1.153 geçti / 5 atlandı, JS 211/211.
MainWindow.xaml.cs 3.999 satır. Bekleyen: commit bölme (yarım, 71 değişiklik commit'siz) ve
launcher-bypass genelleştirme (keşif tamam, uygulama yok).
