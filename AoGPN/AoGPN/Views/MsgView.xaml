<reactiveui:ReactiveUserControl
    x:Class="AoGPN.Views.MsgView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveui="http://reactiveui.net"
    xmlns:resx="clr-namespace:ServiceLib.Resx;assembly=ServiceLib"
    xmlns:vms="clr-namespace:ServiceLib.ViewModels;assembly=ServiceLib"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="vms:MsgViewModel"
    Style="{StaticResource ViewGlobal}"
    mc:Ignorable="d">
    <DockPanel Margin="8">
        <!-- Toolbar card -->
        <Border
            Margin="0,0,0,8"
            Padding="14,12"
            Background="{StaticResource GlassPanelBg}"
            BorderBrush="{StaticResource GlassEdgeBrush}"
            BorderThickness="1"
            CornerRadius="16"
            DockPanel.Dock="Top">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                    <materialDesign:PackIcon
                        Width="20"
                        Height="20"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center"
                        Foreground="{StaticResource GpnCyan}"
                        Kind="MessageTextOutline" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="{DynamicResource StdFontSize4}"
                        FontWeight="SemiBold"
                        Text="{x:Static resx:ResUI.MsgInformationTitle}" />
                </StackPanel>
                <WrapPanel Margin="0,10,0,0" Orientation="Horizontal">
                    <ComboBox
                        x:Name="cmbMsgFilter"
                        Width="200"
                        Margin="{StaticResource MarginLeftRight8}"
                        materialDesign:HintAssist.Hint="{x:Static resx:ResUI.MsgFilterTitle}"
                        materialDesign:TextFieldAssist.HasClearButton="True"
                        AutomationProperties.Name="{x:Static resx:ResUI.MsgFilterTitle}"
                        IsEditable="True"
                        Style="{StaticResource DefComboBox}" />
                    <Button
                        x:Name="btnCopy"
                        Width="24"
                        Height="24"
                        Margin="{StaticResource MarginLeftRight8}"
                        AutomationProperties.Name="{x:Static resx:ResUI.menuMsgViewCopyAll}"
                        Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                        ToolTip="{x:Static resx:ResUI.menuMsgViewCopyAll}">
                        <materialDesign:PackIcon VerticalAlignment="Center" Kind="ContentCopy" />
                    </Button>
                    <Button
                        x:Name="btnClear"
                        Width="24"
                        Height="24"
                        Margin="{StaticResource MarginLeftRight8}"
                        AutomationProperties.Name="{x:Static resx:ResUI.menuMsgViewClear}"
                        Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                        ToolTip="{x:Static resx:ResUI.menuMsgViewClear}">
                        <materialDesign:PackIcon VerticalAlignment="Center" Kind="Delete" />
                    </Button>
                    <TextBlock
                        Margin="{StaticResource MarginLeftRight8}"
                        VerticalAlignment="Center"
                        Style="{StaticResource ToolbarTextBlock}"
                        Text="{x:Static resx:ResUI.TbAutoRefresh}" />
                    <ToggleButton
                        x:Name="togAutoRefresh"
                        Margin="{StaticResource MarginLeftRight8}"
                        HorizontalAlignment="Left"
                        AutomationProperties.Name="{x:Static resx:ResUI.TbAutoRefresh}"
                        IsChecked="True" />
                    <TextBlock
                        Margin="{StaticResource MarginLeftRight8}"
                        VerticalAlignment="Center"
                        Style="{StaticResource ToolbarTextBlock}"
                        Text="{x:Static resx:ResUI.TbAutoScrollToEnd}" />
                    <ToggleButton
                        x:Name="togScrollToEnd"
                        Margin="{StaticResource MarginLeftRight8}"
                        HorizontalAlignment="Left"
                        AutomationProperties.Name="{x:Static resx:ResUI.TbAutoScrollToEnd}"
                        IsChecked="True" />
                </WrapPanel>
            </DockPanel>
        </Border>

        <!-- Terminal -->
        <Border
            Background="{StaticResource GlassPanelBg}"
            BorderBrush="{StaticResource GlassEdgeBrush}"
            BorderThickness="1"
            CornerRadius="16">
            <DockPanel>
                <Border
                    DockPanel.Dock="Top"
                    Padding="12,7"
                    Background="#0D1117"
                    BorderBrush="#222B38"
                    BorderThickness="0,0,0,1"
                    CornerRadius="15,15,0,0">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" Margin="0,0,6,0" Fill="#FF5F57" />
                            <Ellipse Width="10" Height="10" Margin="0,0,6,0" Fill="#FEBC2E" />
                            <Ellipse Width="10" Height="10" Margin="0,0,10,0" Fill="#28C840" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontFamily="Consolas"
                                FontSize="12"
                                Foreground="#8B949E"
                                Text="AO GPN Terminal" />
                        </StackPanel>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="Consolas"
                            FontSize="11"
                            Foreground="#28C840"
                            Text="● CANLI" />
                    </DockPanel>
                </Border>
                <Border Background="#0B0F17" CornerRadius="0,0,14,14">
                    <RichTextBox
                        x:Name="txtMsg"
                        AcceptsReturn="True"
                        Background="Transparent"
                        BorderThickness="0"
                        FontFamily="Consolas"
                        FontSize="{DynamicResource StdFontSize}"
                        Foreground="#E6EDF3"
                        HorizontalScrollBarVisibility="Auto"
                        IsReadOnly="True"
                        IsUndoEnabled="False"
                        VerticalScrollBarVisibility="Visible">
                        <RichTextBox.ContextMenu>
                            <ContextMenu Style="{StaticResource DefContextMenu}">
                                <MenuItem
                                    x:Name="menuMsgViewSelectAll"
                                    Height="{DynamicResource MenuItemHeight}"
                                    Header="{x:Static resx:ResUI.menuMsgViewSelectAll}"
                                    InputGestureText="Ctrl+A" />
                                <MenuItem
                                    x:Name="menuMsgViewCopy"
                                    Height="{DynamicResource MenuItemHeight}"
                                    Header="{x:Static resx:ResUI.menuMsgViewCopy}"
                                    InputGestureText="Ctrl+C" />
                                <MenuItem
                                    x:Name="menuMsgViewCopyAll"
                                    Height="{DynamicResource MenuItemHeight}"
                                    Header="{x:Static resx:ResUI.menuMsgViewCopyAll}" />
                                <MenuItem
                                    x:Name="menuMsgViewClear"
                                    Height="{DynamicResource MenuItemHeight}"
                                    Header="{x:Static resx:ResUI.menuMsgViewClear}" />
                            </ContextMenu>
                        </RichTextBox.ContextMenu>
                    </RichTextBox>
                </Border>
            </DockPanel>
        </Border>
    </DockPanel>
</reactiveui:ReactiveUserControl>
